# FLIPORAMA – simple local flipbook sheet printer

Print-ready PDF with 4×3 grid (12 frames per 8.5×11 sheet), center-cropped, EXIF-sorted by default, 0.25" margins, 300 dpi effective.

Run:
```bash
pip install -r requirements.txt
streamlit run app.py